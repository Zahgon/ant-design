import React, { useState } from 'react';
import { SmileOutlined } from '@ant-design/icons';
import {
  Cascader,
  ConfigProvider,
  Divider,
  List,
  Select,
  Space,
  Switch,
  Table,
  Transfer,
  TreeSelect,
} from 'antd';

const customizeRenderEmpty = () => { throw new Error("STUB"); };

const style: React.CSSProperties = { width: 200 };

const App: React.FC = () => {
    throw new Error("STUB");
};

export default App;
