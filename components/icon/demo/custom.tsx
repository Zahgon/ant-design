import React from 'react';
import Icon, { HomeOutlined } from '@ant-design/icons';
import { Space } from 'antd';
import type { GetProps } from 'antd';

type CustomIconComponentProps = GetProps<typeof Icon>;

const HeartSvg: React.FC = () => { throw new Error("STUB"); };

const PandaSvg: React.FC = () => { throw new Error("STUB"); };

const HeartIcon: React.FC<Partial<CustomIconComponentProps>> = (props) => { throw new Error("STUB"); };

const PandaIcon: React.FC<Partial<CustomIconComponentProps>> = (props) => { throw new Error("STUB"); };

const App: React.FC = () => { throw new Error("STUB"); };

export default App;
