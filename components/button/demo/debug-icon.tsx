import React from 'react';
import { ArrowDownOutlined, MinusSquareOutlined, SearchOutlined } from '@ant-design/icons';
import { Button, ConfigProvider, Divider, Flex, Input, Radio, Tooltip } from 'antd';
import type { ConfigProviderProps } from 'antd';
import { FiColumns } from 'react-icons/fi';

type SizeType = ConfigProviderProps['componentSize'];

/**12px 图标 */
const Icon12Size = () => { throw new Error("STUB"); };
/**16px 图标 */
const Icon16Size = () => { throw new Error("STUB"); };
/**不规则宽高  */
const IconIrregularSize = () => { throw new Error("STUB"); };

const App: React.FC = () => {
    throw new Error("STUB");
};

export default App;
