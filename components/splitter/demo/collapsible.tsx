import React, { useState } from 'react';
import { Flex, Splitter, Switch, Typography } from 'antd';
import type { SplitterProps } from 'antd';

const Desc: React.FC<Readonly<{ text?: string | number }>> = (props) => { throw new Error("STUB"); };

const CustomSplitter: React.FC<Readonly<SplitterProps>> = ({ style, ...restProps }) => { throw new Error("STUB"); };

const App: React.FC = () => {
    throw new Error("STUB");
};

export default App;
